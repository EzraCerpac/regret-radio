{
  "name": "regret-radio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port ${PORT:-5173}",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port ${PORT:-4173}",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:browser": "playwright test",
    "fixture:export": "julia --project=tools/exporter tools/exporter/export.jl",
    "fixture:verify": "julia --project=tools/exporter tools/exporter/export.jl --verify",
    "ci": "bun run typecheck && bun run test && bun run build && bun run test:browser",
    "check": "bun run typecheck && bun run test && bun run build && bun run test:browser && bun run fixture:verify"
  },
  "dependencies": {
    "@fontsource-variable/anybody": "5.2.7",
    "@fontsource/ibm-plex-mono": "5.2.7",
    "@fontsource/ibm-plex-sans": "5.2.7"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "vite": "8.1.4",
    "vitest": "4.1.10"
  }
}
